import React from "react";
import { Container, Typography } from "@mui/material";

const Home = (props) => {
    return (
        <Container>
            <Typography>Home Page</Typography>
        </Container>
    )
}

export default Home